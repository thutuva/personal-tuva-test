name: Generate Markdown

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  generate_markdown:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.x

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install PyYAML

    - name: Generate Markdown
      run: |
        python .github/generate_markdown.py
        
    # - name: Commit and Push Markdown
    #     run: |
    #       git add output.md
    #       git commit -m "Add generated Markdown"
    #       git push
